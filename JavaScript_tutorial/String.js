let namee = "Harshal"
// Template Literals -> It uses backticks instead of quotes to define string 
//  In template literals it is possible to use both single and double quotes inside the string.

// Sting Interpolation ->> We can use variables inside template literals 

console.log(`"My name is ${namee}"` )  // --> THis is called String interpolation


/* String Methods
*/

let a = "Harshal"
console.log(a.toUpperCase())
console.log(a.toLowerCase())
let friend = "Abhishek"
console.log(a.concat(" is friend of ",friend))

let g = "        Rohit"
console.log(g.trim())